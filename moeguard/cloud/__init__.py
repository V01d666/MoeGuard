"""v2 云能力实验层。

MVP 不构建云端 UI，也不启用 AI 对话、BYOK、形象生成、TTS 或推送。
这里的模块只保存技术实验与未来接口骨架；正式启用前必须重新通过凭据、
数据流、隐私、计费和维护成本门禁。

注意：client 依赖 requests（cloud extra），按需导入，避免拉起额外依赖。
内部使用前需单独安装 ``requests>=2.31``；公开基础版不携带本模块。
"""
